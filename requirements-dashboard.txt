# Streamlit dashboard runtime dependencies.
# Phase 2: installed locally to run the dashboard. Phase 3: these become the
# dependencies baked into the dashboard's Docker image.
streamlit==1.57.0
pandas==3.0.3
pyarrow==24.0.0
# s3fs lets pandas.read_parquet read s3:// URIs directly via fsspec.
# Phase 3 ECS task sets WISTIA_GOLD_URI=s3://.../gold; the dashboard reads
# Gold Parquet straight from S3, no local download.
s3fs==2026.4.0
