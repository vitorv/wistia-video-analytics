"""Streamlit dashboard — reads the Gold star schema and renders engagement views.

Phase 2 runs the dashboard locally on Parquet files produced by
``python -m src.transforms``. Phase 3 packages the same code into a Docker
image deployed to ECS Fargate.
"""
