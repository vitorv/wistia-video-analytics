# ai files
context_vault/
CLAUDE.md
.claude/

# secrets
.env

# assignment doc — contains the shared API token; keep local only
docs/Requirement Document - Wistia Video Analytics.md

# api exploration responses (may contain sensitive data)
scripts/api_testing/api_responses/

# python
__pycache__/
*.pyc
.venv/
venv/

# tooling caches & coverage artifacts
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# local landing zone (mirrors future s3://bucket/landing/)
landing/

# medallion layer outputs (mirror future s3://bucket/<layer>/)
bronze/
silver/
gold/

# vendored Hadoop native binaries for local Spark on Windows (see README)
vendor/

# Lambda packaging artifacts (zip + staging tree built by infra/scripts/package-lambda.ps1)
build/
